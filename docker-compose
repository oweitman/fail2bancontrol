version: '3.9'

services:
    fail2banwebcontrol:
        image: fail2banwebcontrol:latest
        container_name: fail2banwebcontrol

        ports:
            - '9191:9000'
        volumes:
            # Sock-Datei vom Host einbinden (Pfad anpassen!)
            - ./path/to/socket.sock:/path/in/container/socket.sock
        environment:
            TZ: Europe/Berlin
        restart: unless-stopped
